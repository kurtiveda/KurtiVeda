const categories = {
  name: "Category",
  title: "Category",
  type: "document",
  fields: [
    {
      name: "title",
      title: "Title",
      type: "string",
      description: "The name of the category",
    },
    // Add more fields as needed
  ],
};

export default categories;
